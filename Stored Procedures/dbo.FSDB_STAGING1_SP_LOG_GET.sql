SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO


-- NS 6/15/2016

CREATE PROCEDURE [dbo].[FSDB_STAGING1_SP_LOG_GET]
	@WHAT_LOADED VARCHAR(50),
	@LOAD_STATUS VARCHAR(50),
	@LOAD_DESC VARCHAR(200)
AS
BEGIN


INSERT INTO dbo.webservices_log
(
	POST_OR_GET,
	WHAT_LOADED,
	LOAD_STATUS_CD,
	LOAD_STATUS_DESC,
	POST_DATE
)
VALUES 
(
	'GET',
	@WHAT_LOADED, 
	@LOAD_STATUS, 
	@LOAD_DESC,
	GETDATE()	
)

END

GO
